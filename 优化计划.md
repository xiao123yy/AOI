# AOI 三大问题优化计划

> 面向 flash 的实现计划。每条优化项标注：现状 → 问题 → 具体改法 → 涉及文件/函数 → 验收标准。
> 按三大问题组织，最后给出实施顺序 checklist。

---

## 优先级总表

| # | 问题 | 优化项 | 风险 | 适合 flash 直接实现 | 预期收益 | 状态 |
|---|---|---|---|---|---|---|
| 2.1 | 问题2 | **阈值校准稳健化** | 低 | ✅ | 修复 FPR 11~13% → ≤6% | ✅ 已实现（30% 校准+margin）；机制生效，transistor FPR 受模型上限所限（见执行记录） |
| 3.1 | 问题3 | **修复死参数 + 重训后验证/回滚** | 低 | ✅ | 补全"历史回放+验证+回滚" | ✅ 已实现，链路实测 4/4 分支通过（含尺度感知漂移守卫） |
| 1.1 | 问题1 | 高分辨率 CPU 预处理优化 | 中 | ✅ | 2500²端到端 191→~150ms | ✅ 已实现，实测 50.3ms（远超目标） |
| 2.2 | 问题2 | zero-shot 命令封装 | 低 | ✅ | 交付"无样本启动" | ✅ 已实现，grid 实测 FPR 4% / 检出 96.7% |
| 2.3 | 问题2 | 5 类统一新基线 | 低 | ✅ | 一致的可迁移性结论 | ✅ **5/5 全部完成**（grid/leather/capsule/transistor/dagm Class3），见执行记录 2.3 与 项目详解.md §16.8 |
| 2.7 | 问题2 | 训练可复现性 | 低 | ✅ | 实验可比 | ✅ 已实现（seed/worker RNG/几何缓存/num_workers） |
| C1 | 跨问题 | 评估加 threshold sweep | 低 | ✅ | 看清模型上限 vs 部署点 | ✅ 已实现，已用于 2.1 归因 |
| 2.4 | 问题2 | E2 结构监督开关 + ablation | 中 | ⚠️ 需跑实验 | 确认对外观类是否有害 | ⏸ 待 2.3 基线后决定 |
| 2.5 | 问题2 | LOCO 部件 mask 语义对齐 | 高 | ❌ 研究 | transistor 类再提精度 | ⏸ 研究项 |
| 2.6 | 问题2 | 教师蒸馏 | 高 | ❌ 研究 | 弱类别提精度 | ⏸ 研究项 |
| 1.2 | 问题1 | 视频时序头训练 | 高 | ❌ 研究 | 视频逻辑异常检测 | ⏸ 研究项 |
| C2 | 跨问题 | C²清理 / git 修复 | 低 | ✅ | 维护负担↓ | ✅ C² 改配置守卫（保留文件）；git 需用户手动一条命令 |
| C3 | 跨问题 | 实验目录化 | 低 | ✅ | 实验可追溯 | ⚠️ 部分：`experiments/` 已建（每类独立目录+summary.csv）；`config_*.json` 不搬动（相对路径会断） |
| C4 | 跨问题 | 部署产物一致性 | 低 | ✅ | 评估不失效 | ✅ 已实现（`deploy_threshold` 落盘 + `check-deploy` 命令） |

---

## 问题 1：实时检测（`modules/realtime_detection.py`）

### 1.1 高分辨率端到端延迟：CPU 预处理是剩余瓶颈

**现状**：2500×2500 输入端到端 ~191ms（RTX 5060），已逼近 200ms 硬指标。骨干前向只有 ~12ms，大头在 CPU 图像解码 + 特征统计。`utils/image.py` 里 `load_rgb`（PIL 解码）+ `lab_statistics`/`geometry_statistics` 各自 `image.convert("RGB")` + `np.asarray`，同一张 2500² 图被**全图转换 3 次**。

**改法**（`utils/image.py` + `realtime_detection.py:inspect_image`）：
1. 新增 `load_rgb_array(path) -> np.ndarray`，用 `cv2.imdecode`（`cv2.IMREAD_COLOR` + `COLOR_BGR2RGB`）替代 PIL，解码更快且免 PIL↔numpy 往返；
2. `lab_statistics` / `geometry_statistics` 改为**接受 ndarray**（或加 array 版本），`inspect_image` 里解码一次、两个统计函数共享同一数组；
3. `lab_statistics` 的 3 通道 `cv2.calcHist` 循环改成一次 `np.histogram` 向量化（或 `cv2.calcHist` 一次传 3 通道再 marginalize）；
4. `geometry_statistics` 降采样阈值 `GEOMETRY_FEATURE_MAX_SIDE` 1024→768 试降（Canny 是大头）；
5. **保留 `load_rgb`（PIL 版）不动**——训练侧 `normalize_image` 等仍用它，避免波及全项目。

**验收**：2500×2500 单图 `latency_breakdown_ms.other` 明显下降，端到端 <150ms；几何/LAB 特征余弦相似度 ≥0.999（与旧实现比，防止精度回退）。

> ⚠️ 附：赛题硬指标是 **RTX 2060 <200ms**，当前数据全在 5060 上。flash 实现后建议在 2060（或 `torch.set_default` 降级）实测一次，给出真实余量；若不够，追加 fp16/TensorRT 量化（`_forward_pil` 已用 autocast，但模型加载未转 half）。

### 1.2 视频时序头未训练（研究项，非本次实现）

`TemporalLogicHead` 存在但 `temporal_head.pth` 从未生成、无训练命令，视频退化为逐帧 max。无视频数据集，建议**如实标注"未实现"**或后续用 LOCO logic_anomaly 造序列再训练。**本次不做。**

### 1.3 ROI 精修（诊断已证 E0 有害，仅记录）

E0 已证 ROI 精修有害（seen 0.87→0.64）。若想救活，方向是"多尺度/原分辨率热力图选 ROI"而非当前 384 全局热力图 top-k。属研究，记录在案即可。

---

## 问题 2：少样本/无样本启动（`fewshot_transfer.py` + `normal_reference.py`）

### 2.1 阈值校准稳健化（**最高优先级**）

**现状**：`fewshot_transfer.py` 的 `adapt()` 里：
- `normal_val_count = max(5, round(len*0.2))` → 100 张只留 **20 张**做校准；
- conformal rank = `ceil((20+1)*0.95)-1 = 19` → **阈值直接等于 20 张里的最高分**。

后果：正常 FPR 实测 11~13%（目标 5%），阈值对校准集里的"最异常正常图"极敏感。这是当前最痛的点。

**改法（组合，全部机械）**：
1. `normal_val_count = max(8, round(len(normal_array)*0.3))`（30 校准 / 70 训练）；
2. 阈值加安全 margin：
   ```python
   threshold = calibration_scores[rank] + max(0.05, 0.1 * calibration_scores.std())
   ```
   并把 margin 系数做成 config 字段 `threshold_margin`（默认 0.0 时保持旧行为，便于对比）；
3. （可选更强）训练结束后用 `train_normal + validation_normal` 全部 100 张重新走 `_combined_scores` 校准，扩大样本量。

**验收**：`evaluate` 的 `normal_fpr ≤ 0.06`，`unseen_recall` 相对当前（0.7）不明显下降。跑 transistor + grid 两类验证。

### 2.2 zero-shot 命令封装（交付"无样本启动"）

**现状**：`build_zero_shot_reference`（`fewshot_transfer.py:246`）已实现，但 `main.py` 无命令；`adapt` 有 20 正常+2 异常硬门槛。

**改法**：`main.py` 加 `zero-shot-adapt` 子命令：
- 参数：`--normal-dir`（必填）、`--output-dir`（可选，默认 deployment）；
- 逻辑：`build_model(config, full_checkpoint=industrial_checkpoint)` → `transfer.build_zero_shot_reference(normal_paths)` → 阈值用保守默认（如 0 或公共校准）→ 保存 `target_model.pth` + `normal_reference.pth`。

### 2.3 用 LOCO+VisA 新池统一 5 类基线

**现状**：LOCO+VisA 只对 transistor 跑过 E3；grid/leather/capsule/dagm 都是旧池（无 LOCO）结果，跨类别结论不可比。

**改法**：写 `scripts/run_experiment.py`（参数化 `--target-dataset --target-category --unseen-type`），串起：
```
make-split → pretrain-public（该类别专属 manifest，防泄漏）→ adapt → evaluate → vis_infer
```
每类**独立 workspace**（`workspace/<category>/`），最后汇总一张 metrics 表。

**验收**：5 类 + 最终组合（E3+E2）各跑出 `metrics.json`，汇总 CSV 与 `项目详解.md` 16.2 对齐。

### 2.4 E2 结构监督做成开关 + ablation（需跑实验）

**现状**：`_train_phase`（`fewshot_transfer.py:801-808`）**无条件**加 `0.2*component_loss + 0.05*geometry_loss`。对 transistor（结构类）正面，但 grid/leather（外观类）可能有副作用。

**改法**：config 加 `adapt_component_weight=0.2`、`adapt_geometry_weight=0.05`，替换硬编码；然后对 5 类跑 {开/关} ablation。

**验收**：确定外观类是否应关掉结构监督，把结论写回配置默认值。

### 2.5 LOCO 部件 mask 语义对齐（研究项）

`_load_mask`（`fewshot_transfer.py:53`）把 LOCO 多部件彩色 mask 二值化 `>0.5`，`component_head` 8 槽位与部件无语义对应。改法是把 LOCO 每类部件 mask 转 8 槽 one-hot 对齐 `component_head`，缺件/错位用部件级监督。**工程量大、需实验验证，交给 flash 前先评估是否值得。**

### 2.6 教师蒸馏（研究项）

`teacher_checkpoint`（ConvNeXt-Large 755MB）已下载未用。`_public_loss` 加教师特征/logits 蒸馏损失（教师 inference_mode 冻结），可能提弱类别（transistor）精度。**研究项。**

### 2.7 训练效率 + 可复现性（机械）

1. `PublicIndustrialDataset.__getitem__`（`fewshot_transfer.py:90`）每 epoch 重算 `geometry_statistics`（Canny）：预计算进 manifest 或缓存；
2. `normalize_image` PIL BICUBIC → cv2 插值提速；
3. `TargetDataset.__getitem__`（`fewshot_transfer.py:150,171`）用全局 `np.random`（合成概率/增强），多 worker 不可复现 → 改成 `self.rng = np.random.default_rng(seed)`；
4. `num_workers` 从 2 适当提升。

---

## 问题 3：反馈优化（`feedback_optimization.py`）

### 3.1 修复死参数 + 补全"验证/回滚"（赛题明确要求）

**现状**：`periodic_retrain`（`feedback_optimization.py:160`）签名里有 `validation_normal_paths` 参数，**函数体里从未使用**（死参数）；安全检查只有"阈值漂移 >0.5"，**没有任何验证步骤**，不满足赛题"更新必须经过历史回放、验证和回滚检查"。

**改法**：
1. 重训后加载新模型+参考，用 `AOIRealtimeDetector.evaluate_dataset` 对 `validation_normal_paths`（+ 可选 `validation_anomaly_paths`）算 `normal_fpr` 和 AUROC；
2. 若 `normal_fpr > target_normal_fpr * 1.5` 或 AUROC 较旧模型显著下降 → `raise RuntimeError` 触发已有回滚逻辑；
3. 回滚后把原因写进 `feedback/rollback/rollback.log`，避免静默失败；
4. `main.py:command_feedback_retrain` 已传 `validation_normal_paths`，无需改 CLI，只把函数体接上。

**验收**：模拟 20 条反馈跑 `feedback-retrain`，验证"FPR 超阈值 → 正确回滚 + 保留备份"整条链路。

### 3.2 阈值建议逻辑稳健化

**现状**：`threshold_suggestion`（`feedback_optimization.py:107`）用 `median(fp_scores)+0.05` / `median(fn_scores)-0.05`，±0.05 是绝对量、不随分数尺度自适应，样本少时噪声大。

**改法**：步长改为 `0.05 → 0.2 * std(所有反馈分数)`（自适应）；或直接用 FPR 目标反推阈值（复用问题 2.1 的 conformal 逻辑）。

---

## 跨问题

**C1 评估协议**（`realtime_detection.py:evaluate_dataset`）：现在只报部署固定阈值点指标。加 `--threshold-sweep` 输出"最优 AUROC 点"与"P@FPR=5%"两个口径，避免阈值不稳掩盖模型真实能力（2.1 改完后尤其需要）。

**C2 代码卫生**：
- C²-FFN 已证不可用（AUROC 0.446），删除 `modules/c2_ffn_convnext_v1.py` + `aoi_model.py:48-52`、`fewshot_transfer.py` 里的 C² 分支（或统一标记 `experimental` 并在 config 校验拦截）；
- 修复 git dubious ownership（`git config --global --add safe.directory D:/Admin/Documents/PycharmProject/AOI`），否则无法做实验版本管理。

**C3 实验管理**：`config_*.json` 散落在根目录 → 建 `experiments/` 目录，每个实验一个子目录含 config + 结果；加实验记录（wandb 或简单 JSON 记录超参哈希）。

**C4 部署产物一致性**：当前 `deployment/normal_reference.pth` 阈值 **-0.628** 与 `evaluation/metrics.json` 记录的 **0.011** 不匹配（部署在最后评估后被重新生成，评估已过期）。建议加一个 `deploy` 步骤检查/或每次 adapt 后强制重跑 evaluate，保证评估反映当前部署。

---

## 交给 flash 的实施顺序（checklist）

1. **2.1 阈值校准稳健化**（改 `adapt()` 2 行 + config 字段 + margin）→ 重跑 transistor/grid evaluate 验证 FPR；
2. **3.1 反馈重训验证/回滚**（接上死参数 `validation_normal_paths`）；
3. **1.1 CPU 预处理优化**（`cv2.imdecode` + 复用数组）；
4. **2.2 zero-shot 命令**；
5. **2.7 可复现性 + 数据管线**（传 rng、预计算 geometry、num_workers）；
6. **C1 评估 threshold sweep**；
7. **2.3 5 类统一基线脚本**；
8. **C2/C3/C4 卫生与实验管理**；
9. 实验项（2.4 ablation）跑完后再决定 2.5/2.6 是否投入。

---

## 执行记录（2026-08-19）

按 checklist 顺序实测的结果与结论（数字以当时记录为准）。

### 2.1 阈值校准稳健化

- **改法落库**：`normal_val_count = max(8, round(len*0.3))`；阈值 = conformal 分位 +
  `threshold_margin · max(0.05, 0.1·std(校准分))`；`threshold_margin` 配置字段（默认 1.0，0=旧行为）；
- **口径澄清**：5% FPR 下有限样本 conformal 的"取最值"性质——n≤20 时 rank 恒为最高分，
  n=30 时仍是最高分（rank=29/30）；20%→30% 的实作用是 margin 的 std 估计更稳；
- **transistor 实测（重适配后部署，阈值 0.093817）**：评估 80 张（60 正常 + 10 seen + 10 unseen）
  → 正常 FPR **13.3%**（8/60）、seen 召回 0.8、unseen 召回 **0.4**、Overall AUROC 0.8183
  （seen 0.84 / unseen 0.7967）、P/R/F1 0.60、延迟 29.9/34.0 ms；
- **threshold sweep（C1）**：最优 F1 点阈值 -1.1627（P 0.463 / R 0.95 / F1 0.623）；
  P@FPR=5% 点阈值 0.4709（P 0.70 / R 0.35）——**没有任何阈值同时满足 FPR≤6% 与 unseen 召回≈0.7**；
- **归因（如实记录，未达标）**：瓶颈是模型不是阈值。query 正常图因分布偏移系统性偏高分
  （support 正常分 max 3.03 / std 1.05；query 正常分 top8 全部 ≥ 旧阈值 0.0938），
  而 unseen（引脚弯折）与正常分重叠严重——support 侧任何校准都修不掉该偏移；
- 100 张全量重校准试验更差（FPR 21.7%），不采用；grid 侧 2.1 验证并入 2.3 统一基线（见 16.8/执行记录 2.3）。

### 3.1 反馈重训验证/回滚

- **死参数修复**：`feedback-retrain` 的 `--validation-normal-dir` 现为必填，重训前后都跑验证集（FPR+AUROC）；
- **漂移守卫改造**：旧 0.5 绝对上限会确定性拦截一切更新（重训 logit 尺度整体平移 0.5~1.76 属正常）
  → 改为**尺度感知**：`limit = max(0.5, 4.0 × 旧验证集正常分数 std)`（宽松哨兵，真闸门是 FPR/AUROC）；
- **链路实测 4/4 通过**（隔离工作区，20 条模拟反馈，transistor 部署为底）：
  - 端到端：重训后新模型把 top8 误报从 8/8 降到 5/8（FPR 0.625 > 7.5%）→ **FPR 守卫回滚**，
    部署哈希不变、备份保留、rollback.log 留痕（error 含数值）；
  - 单元分支：FPR=0.3 → 回滚；阈值 +5.0（> 2.21=4×0.55）→ 漂移守卫回滚；
    AUROC 0.9→0.1 → 回滚；
- 备注：早期一次回滚曾出现 `error:""`（异常被 except 吞且 str 为空），同输入重跑无法复现，
  判定为偶发/瞬时异常（回滚机制仍正确兜底）；脚本日志读取须用 `encoding="utf-8"`（GBK 默认会崩）。

### 1.1 CPU 预处理优化（含基准）

- `utils/image.py` 全量向量化：`load_rgb_array`（cv2 单次解码）、`normalize_array`（PIL BICUBIC，
  像素级一致）、`lab_statistics_array`（bincount）、`geometry_statistics_array`（边界可配置）；
- 2500×2500 单图端到端：旧 191ms → **50.3ms（median）/ 53.0ms（P95）**；
  拆解：骨干前向 11.9ms、正常参考 ~0、ROI 0、other 38.0ms；
- **复核（2026-08-19，当前 transistor 部署）**：同一 2500×2500 口径复测 21 次 →
  噪声图 median 61.0ms / 真实图放大 median 62.9ms（P95 ≈ 66ms），骨干前向 ~15-16ms；
  较 1.1 基准高约 10ms，属机器态（GPU/CPU 时钟与热状态）与输入图差异带来的处理波动，
  流水线代码未回退（同为 1.1 优化后的路径）；
- 特征一致性：几何/LAB 相对旧实现 **逐位一致（diff 0.0）**。

### 2.2 zero-shot 命令

- `main.py zero-shot-adapt --normal-dir ...`：不训练，≥5 张正常图建库 + conformal+margin 定阈值；
- **grid 实测**（100 张正常）：rank=95、margin 0.1151、阈值 -4.6301；
  60 正常 + 30 异常 query → FPR **4%**、异常检出 **96.7%**（29/30）。

### 2.7 / C1 / C4 / C2 / C3

- **2.7**：`TargetDataset` 与合成引擎接受外部 rng/seed；`geometry_statistics` 结果缓存；
  `num_workers` 配置可复现（`config_nw0.json`）；
- **C1**：`evaluate --threshold-sweep` 输出最优 F1 点 + P@FPR=target% 两档口径，已用于 2.1 归因；
- **C4**：`check-deploy` 实测 `consistent: true`——用户清理后已从 2.3 的 transistor 实验重建
  `aoi_full_workspace/deployment/`（2 个 pth）+ `evaluation/metrics.json`，
  复测部署阈值 **1.3281853510857198 == metrics.json** 一致（2026-08-19）；
  `evaluate_dataset` 新增 `deploy_threshold` 与 `normal_score_std` 落盘；
- **C2**：C² 留骨干文件 + config 校验拦截（不可用已实证）；git safe.directory 需用户手动执行
  `git config --global --add safe.directory D:/Admin/Documents/PycharmProject/AOI`（沙箱无权限，勿再尝试提权）；
- **C3**：`experiments/` 每类独立 workspace + `summary.csv` 已落地；`config_nw0.json` 等根目录配置
  不搬动（相对路径会断，仅文档说明）。

### 2.3 五类统一基线（已完成）

- `scripts/run_experiment.py --all --base-config config_nw0.json`，**5/5 全部跑完**
  （≈43 分钟/类，总 ~3.6h；每类独立 `experiments/<tag>/`：config/checkpoints/deployment/evaluation）；
- 首跑发现两问题并处理：`UnboundLocalError`（pretrain 后误 `del transfer`，已修复）+ **D 盘写满**
  （用户自行清理出 4GB 后，已删我的失败残留并重跑）；
- **结果（详见 项目详解.md §16.8 / 比赛技术报告.md §14.5 / summary.csv，全为 2026-08-19 同配置）**：
  - **grid**：AUROC 1.0/1.0/1.0，FPR 0%（0/21），seen/unseen 召回 100% —— **达标**；
  - **dagm Class3**：AUROC 0.998，FPR 5.1%（25/491），seen 召回 99.2%（119/120）—— **达标**
    （dagm 缺陷无类型标签，unseen_type=null、unseen 指标为 NaN，按整体检测口径）；
  - **leather**：AUROC 0.999，FPR 9.4%（3/32）略超 6% 线（30 张校准样本的类别方差），召回 100%；
  - **capsule**：AUROC 0.876，FPR 8.7%（2/23）略超，seen/unseen 召回 68%/65%（模型上限难类，best-F1 0.909）；
  - **transistor**：AUROC 0.858，部署点 FPR 0%（0/60）但 bent_lead 召回仅 10%（阈值过严）；
    且 **本轮 split 的 query_seen=0**（bent_lead 全 10 张进 unseen、其余异常类型全进 support，
    seen 指标 NaN，与 2.1 旧池 seen 30/unseen 20 划分不同）；
  - **小结 3/5 类达标**；leather/capsule 属校准方差、transistor 属模型上限——如实记录，不粉饰。

### 2.8 尺度异常分支 + 「CUDA bad allocation」环境坑（2026-08-21）

**背景**：新增**显式尺度异常分支**——`scale_head`（输出 log(sx), log(sy)）+ 无黑边中心缩放干预合成
（`synthetic_engine.scale_intervene`，iso/aniso、比例 0.8/0.9/1.1/1.2、`BORDER_REFLECT_101` 回填），
`scale_magnitude = |log-scale|` 作为尺寸偏差信号接入融合（fusion 6→7 维）；公共预训练与目标适配
均带尺度回归损失（`public_scale_weight=0.3`、`scale_intervention_probability=0.12`）。

**新 unseen 实验（capsule / squeeze 挤压变形）**：

- 预训练第一步 forward 报 `RuntimeError: bad allocation`，排查链：
  1. 显存正常（RTX 5060 8GB，free 6.3GB）、无残留 python 进程；
  2. 最小复现定位：`no_grad` batch=8 forward 正常；**带梯度训练 batch=1/2/4 正常、batch=8 崩**；
  3. 根因：**系统虚拟内存（commit charge）仅剩 ~5.3GB**（34.4/39.8GB，pagefile 仅 8GB；
     PyCharm 6GB + ToDesk 3.4GB + ChatGPT×2 等把 commit 吃掉）——Windows WDDM 下 cudaMalloc
     会**同步 commit 等量系统虚拟内存**，训练 batch=8 预留 ~2.5-3GB 显存即把剩余 commit 撞爆，
     表现为驱动级 `fatal: Memory allocation failure`（torch 侧呈现为 bad allocation / CUDA unknown error）。
  4. 处理：新增 `config_nw0_b4.json`（batch=4）绕开上限（用户选定方案，未扩 pagefile/关进程）。
- 顺带修两个 scale 损失 bug：`scale_valid` 是 float32 却当 bool 索引（`IndexError`）→ `.bool()`；
  `batch["scale_log"]` 在 CPU 被 cuda 索引 → `.to(device)` 后再索引（两处：`_public_loss` 与 `adapt`）。
- **结果**（batch=4，`experiments/mvtec_ad_capsule_unseen_squeeze/`，wall 2440s）：
  Overall **0.9015** / Seen **0.8909** / Unseen **0.9326**（squeeze 未见缺陷）、F1 0.855（best-F1 0.926）、
  正常 FPR 17.4%（23 张 query 正常图）、延迟 30.9/35.4 ms。unseen 0.93/召回 90% 明显优于同类别 2.3
  crack unseen（0.864/65%），提示尺度分支对尺寸类异常有正面作用；scale_head 独立消融列为后续研究项。

**经验沉淀**：系统多个大户（IDE/远程/IM）占满虚拟内存 commit 时，CUDA 报错不是 "out of memory"
而是 `bad allocation` / `fatal: Memory allocation failure`；批量训练起步前先看 `nvidia-smi` 之外
还要看 **commit charge（Get-CimInstance Win32_OperatingSystem: CommittedBytes vs TotalVirtualMemorySize）**；
降 batch 是最直接绕开手段，根治需扩 pagefile（管理员权限）或释放大户进程。

### 环境与数据说明

- 2026-08-19 磁盘清理删除了 `aoi_full_workspace/` 的运行产物与 6 个历史部署备份、
  `model/convnext_large_22k_1k_224.pth`（教师权重，预留未用）；对应指标均已记录在文档；
- 清理后已从 2.3 的 transistor 实验重建 `aoi_full_workspace/deployment/` 与 `evaluation/metrics.json`，
  `check-deploy` 复测 `consistent: true`（C4 新证据）；根目录残留 `.test_feedback.py` 与孤儿 pyc 已删除。
- 当前（重跑后）的有效工件：`experiments/<tag>/`（每类 config/checkpoints/deployment/evaluation + summary.csv）。
- 2026-08-21 清理：删除根目录遗留配置 `config_c2.json` / `config_capsule_512.json` / `config_roi.json` /
  `config_trans_512.json` / `config_trans_sup.json`（早期试验遗留、workspace 不存在、无代码引用），
  以及临时脚本 `.check_scale_dist.py` 与全部 `__pycache__`；保留 `config_nw0.json`（基线）与
  `config_nw0_b4.json`（batch=4，2.8 实验实际使用）。
