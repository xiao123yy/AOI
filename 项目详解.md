# AOI 项目完全详解

> 本文档从零开始、事无巨细地讲解当前仓库的 AOI（Automated Optical Inspection，自动光学检测）系统：
> 它要解决什么问题、系统怎么搭的、每个模块的代码到底在做什么、每个问题具体是怎么被解决的。
> 所有描述只针对**当前仓库里的代码**（`main` 分支），不涉及未实现的设想。

---

## 目录

1. [这个项目是什么](#1-这个项目是什么)
2. [赛题与系统总览](#2-赛题与系统总览)
3. [目录结构逐文件说明](#3-目录结构逐文件说明)
4. [核心概念：多尺度特征与正常参考](#4-核心概念多尺度特征与正常参考)
5. [模型结构详解（aoi_model.py）](#5-模型结构详解aoi_modelpy)
6. [问题一：实时检测怎么解决](#6-问题一实时检测怎么解决)
7. [问题二：少样本/无样本启动怎么解决](#7-问题二少样本无样本启动怎么解决)
8. [问题三：用户反馈优化怎么解决](#8-问题三用户反馈优化怎么解决)
9. [数据划分：seen / unseen 实验设计](#9-数据划分seen--unseen-实验设计)
10. [合成异常引擎](#10-合成异常引擎)
11. [阈值是怎么定的](#11-阈值是怎么定的)
12. [延迟与精度：2500×2500 高分辨率输入怎么办](#12-延迟与精度25002500-高分辨率输入怎么办)
13. [可视化工具 vis_infer.py](#13-可视化工具-vis_inferpy)
14. [命令行速查：8 个命令分别对应什么](#14-命令行速查8-个命令分别对应什么)
15. [配置文件全字段说明](#15-配置文件全字段说明)
16. [当前实验结果](#16-当前实验结果)
17. [关键实现细节速查表](#17-关键实现细节速查表)

---

## 1. 这个项目是什么

一句话总结：

> 这是一个**模块化的工业质检（AOI）在线检测系统**。新产线只有 100 张正常 + 30 张异常图片可用时，
> 系统通过"先在公开工业数据集上预训练 → 再用少量目标样本迁移适配 → 冻结部署实时检测 →
> 用户反馈驱动优化"四个阶段，实现对产品缺陷（尤其是**训练时从未见过的缺陷类型**）的自动检出。

它回答赛题的三个问题：

| 问题 | 一句话 | 对应代码 |
|---|---|---|
| **问题 1**：实时检测 | 2500×2500 高分辨率图片/视频，低延迟下检出异常并输出热力图 | `modules/realtime_detection.py` |
| **问题 2**：少样本/无样本启动 | 新产线只有 100 正常 + 30 异常图片，要快速适配且能检出未见缺陷 | `modules/fewshot_transfer.py`、`modules/normal_reference.py`、`modules/synthetic_engine.py` |
| **问题 3**：反馈优化 | 部署后用户反馈（误报/漏报）不能直接改线上模型，要走独立更新 + 回滚 | `modules/feedback_optimization.py` |

三个问题的关系：**问题 2 解决"怎么上线"（冷启动），问题 1 解决"上线后怎么跑得快又准"（运行），问题 3 解决"上线后怎么变好"（迭代）**。

---

## 2. 赛题与系统总览

### 2.1 赛题约束（README 第 2 节）

- 输入支持**图片和视频**；
- 原始工业图像分辨率可到 **2500×2500**；
- RTX 2060 或以下 GPU 单图推理目标 **< 200 ms**；CPU 目标 **< 2 s**；
- 新产线适配阶段允许 **100 张正常 + 30 张异常**；
- 适配后，在 1000+ 测试样本上**冻结模型**，测试数据不参与训练；
- 用户反馈只在**独立更新阶段**使用；
- 更新后必须经过**历史回放、验证和回滚检查**再发布。

### 2.2 系统四个阶段（README 第 5 节 + main.py 的命令）

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ 阶段一       │    │ 阶段二       │    │ 阶段三       │    │ 阶段四       │
│ 公共工业预训练│ →  │ 目标少样本适配│ →  │ 冻结实时检测  │ →  │ 用户反馈优化  │
│             │    │             │    │             │    │             │
│ make-split  │    │ adapt       │    │ infer-image │    │ feedback    │
│ pretrain-   │    │             │    │ infer-video │    │ feedback-   │
│ public      │    │             │    │ evaluate    │    │ retrain     │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
      │                  │                  │                  │
 四公开数据集        target_model.pth   deployment/ 冻结      feedback.jsonl
 (MVTec/VisA/       + normal_reference  推理，阈值不变         + rollback 备份
  DAGM/LOCO)        .pth
```

**核心设计思想**：*异常检测 = 监督判别（学"什么是异常"）+ 正常性建模（学"正常长什么样"）*。
监督部分由多任务头的神经网络承担（可在少样本下微调），正常性部分由**正常参考库**承担（不参与梯度训练，纯特征记忆），两者分数融合后与阈值比较得到最终判定。即使从未见过某类缺陷（unseen），只要它偏离了"正常"特征分布，参考库路径就能把它抓出来——这是解决"未见缺陷泛化"的关键。

---

## 3. 目录结构逐文件说明

```
AOI/
├── main.py                  # 命令行统一入口，8 个命令（make-split / pretrain-public /
│                            #   adapt / evaluate / infer-image / infer-video /
│                            #   feedback / feedback-retrain）
├── aoi_model.py             # 核心模型：AOIMultiBranchModel（1 个骨干 + 6 个任务头）
│                            #   + TemporalLogicHead（视频时序头）
├── convnextv2.py            # ConvNeXtV2-Tiny 骨干实现 + 预训练权重加载
├── config.py                # AOIConfig：全部配置项的数据类，读取 config.json
├── config.json              # 当前实验配置（grid 类别、dense 骨干、384 输入等）
├── example_config.json      # 部署用精简配置示例
├── vis_infer.py             # 推理可视化脚本（原图/热力图/F16 激活 三栏对比图）
├── requirements.txt         # 依赖
│
├── modules/                 # 四个核心功能模块
│   ├── realtime_detection.py    # 问题1：实时检测引擎（单图/视频/批量评估）
│   ├── fewshot_transfer.py      # 问题2：公共预训练 + 目标少样本适配 + 阈值校准
│   ├── normal_reference.py      # 正常参考库（局部 token 库 + 马氏距离 + GPU 检索）
│   ├── synthetic_engine.py      # 合成异常引擎（外观/颜色/几何/缺件）
│   ├── feedback_optimization.py # 问题3：反馈记录/即时调阈/周期重训/自动回滚
│   └── c2_ffn_convnext_v1.py    # 实验性 C²-FFN 快速骨干（默认不用）
│
├── utils/
│   ├── paths.py             # 数据集扫描、seen/unseen 划分、清单管理
│   ├── image.py             # 图像加载/归一化、LAB 颜色统计、几何统计、NMS
│   └── manifests.py         # CSV 数据清单读取（预留）
│
├── model/                   # 预训练权重
│   ├── convnextv2_tiny_22k_384_ema.pt   # 学生骨干（ConvNeXtV2-Tiny，110MB）
│   ├── convnext_large_22k_1k_224.pth    # 教师骨干（ConvNeXt-Large，755MB，预留未用）
│   └── model.txt
│
├── data/
│   ├── mvtec_ad/            # MVTec AD（15 类；当前目标类别 grid）
│   └── dagm2007/            # DAGM 2007（10 类纹理缺陷）
│   （visa/、mvtec_loco_ad/ 未下载，扫描代码对缺失目录容错）
│
├── DFMGAN/                  # StyleGAN2 代码（预留未集成，README 说明）
│
└── aoi_full_workspace/      # 运行产出（可删除重建）
    ├── checkpoints/industrial_pretrained.pth + .history.json   # 阶段一产物
    ├── splits/mvtec_ad_grid_unseen_bent/                        # 阶段一数据划分
    │   ├── public_train.jsonl / public_val.jsonl                # 公共训练/验证清单
    │   ├── support/normal（100张）support/anomaly（30张）        # 目标少样本
    │   ├── query/normal（21张）query/seen（15张）query/unseen（12张）
    │   └── summary.json
    ├── deployment/target_model.pth + normal_reference.pth      # 阶段二产物
    ├── evaluation/metrics.json + predictions.csv + failed_cases.csv  # 阶段三评估
    └── feedback/feedback.jsonl + rollback/                      # 阶段四反馈与备份
```

---

## 4. 核心概念：多尺度特征与正常参考

### 4.1 骨干网络：ConvNeXtV2-Tiny（convnextv2.py）

- 结构：`depths=[3,3,9,3]`，`dims=[96,192,384,768]`——4 个 stage，每个 stage 前有下采样层（第一个是 4×4 stride-4 卷积，后面是 2×2 stride-2 卷积）。
- 每个 block = `7×7 深度卷积 → LayerNorm → 1×1 升维(Linear C→4C) → GELU → GRN → 1×1 降维(Linear 4C→C)` + 残差（ConvNeXtV2 特色：用 GRN 替代 LayerNorm 的通道归一化）。
- 前向输出**四个尺度的特征字典**：

| 键 | 形状（384×384 输入） | 语义 |
|---|---|---|
| `f4` | B×96×96×96 | 最浅层，纹理细节 |
| `f8` | B×192×48×48 | 局部结构 |
| `f16` | B×384×24×24 | **局部异常热力图的特征源** |
| `f32` | B×768×12×12 | **全局异常分数的特征源** |

- 权重加载（`load_convnextv2_tiny`）：自动剥掉 `model.`/`module.`/`backbone.`/`encoder.` 前缀，跳过分类头（`head.`/`norm.`）和 `mask_token`；如果骨干层缺失超过 8 个 key 就报错（说明权重格式不匹配）。

### 4.2 正常参考库：NormalReference（normal_reference.py）

这是"正常性建模"的核心数据结构。`fit()` 用 N 张正常图（代码里是目标域的 100 张训练正常图）离线构建，之后推理时冻结不动。它包含 **4 条评分分支**：

| 分支 | 存什么 | 怎么评分 |
|---|---|---|
| `local_bank` | 每张图从 F16 特征图上随机采 128 个 384 维 token，N 张图拼成一个 token 库（上限 12000 个） | 输入图的每个 token 找库中最近邻（欧氏距离），取**每个像素的最小距离**，再取**最大的 1% 平均**作为 `memory_local` 分数 |
| `global` | 每张图 F32 全局池化后的 768 维特征，用 **LedoitWolf 收缩协方差**估计均值 + 精度矩阵 | `(x-μ)ᵀ Σ⁻¹ (x-μ)` 马氏距离 → `memory_global` |
| `color` | LAB 颜色空间统计（3 通道均值 + 3 通道标准差 + 每通道 16 桶直方图 = 54 维） | 马氏距离 → `color` 分数 |
| `geometry` | 几何统计（最大轮廓的外接矩形宽高/位置 + 面积 + 周长，6 维） | 马氏距离 → `geometry` 分数 |

`fit()` 还会计算**校准统计量**（正常图的各分支分数的均值/标准差，`ScoreCalibration`），推理时把各分支原始分数转成 **z-score**（`(x-mean)/std`），让不同分支的量纲可比，才能做加权融合。

> 为什么用马氏距离？工业正常品本身有波动（光照、材质），欧氏距离会把"正常的变化"误判为异常；马氏距离用协方差把正常分布"白化"后再量距离，只有偏离正常分布形状的才叫异常。

**推理时怎么快**：GPU 版本 `score_local_torch` 用平方展开 `||x-y||² = ||x||² + ||y||² - 2xy` 做批量矩阵乘法（分块 4096 个库 token 一次），避免构建 [N, M, C] 三维大数组；token 库常驻 GPU 并缓存范数，只算一次。

---

## 5. 模型结构详解（aoi_model.py）

### 5.1 总体结构

```
                    输入图片 (B,3,384,384)
                           │
              ┌────────────▼────────────┐
              │ backbone: ConvNeXtV2-Tiny│   ← 学生骨干（ImageNet 22K 预训练）
              │ （dense，或实验性 c2_hard_b）│
              └────────────┬────────────┘
                   f16 (B,384,24,24)   f32 (B,768,12,12)
              ┌────────────┴────────────┐
              ▼                          ▼
        local_head (Conv1×1)      global_feature = f32 全局池化
        → 24×24 异常热力图        → 768 维向量
                                        │
                    ┌───────────────────┼──────────────┬──────────────┐
                    ▼                   ▼              ▼              ▼
              global_head          component_head  geometry_head  domain_head
              (768→256→1)          (768→256→8)     (768→256→6)    (768→128→1)
              全局异常分数           8 组件状态       6 维几何回归     real/synthetic
                    │                   │              │           域分类（GRL）
                    └───────────────┬───┴──────────────┘
                                    ▼
                            fusion_head (6→32→1)
                                    │
                            final_logit（最终异常分数）
```

### 5.2 六个头的职责

| 头 | 输入 | 输出 | 作用 |
|---|---|---|---|
| `local_head` | F16 (384 通道) | 1 通道热力图 24×24 | 逐像素"这里是不是异常"，可上采样回原图做定位 |
| `global_head` | F32 池化 (768) | 1 维 logit | 整张图"是不是异常" |
| `component_head` | 768 | 8 维 logits | 8 个组件槽位的状态预测；`sigmoid(p)·(1-sigmoid(p))` 求均值得到"组件不确定性"，组件状态越模糊说明装配越可疑 |
| `geometry_head` | 768 | 6 维回归 | 预测几何统计（尺寸/位置/面积），与真实几何统计的偏差间接反映异常 |
| `domain_head` | 768 | 1 维 | **域对抗头**：区分"真实异常样本 vs 合成异常样本"（训练时用，推理不用） |
| `fusion_head` | 6 维 | 1 维 | 融合 5 路分数 + 外部分数 |

### 5.3 前向与融合公式（`forward`，aoi_model.py:95-169）

1. 骨干出 F16、F32；
2. `local_logits` = local_head(F16) → 热力图；
3. `local_top` = 热力图**前 1%（`local_top_ratio=0.01`）像素的均值**（抓"最可疑的区域"，对大面积异常敏感）；
4. `local_mean` = 热力图全体像素均值（对小而分散的异常敏感）；
5. `global_logit` = global_head(f32 池化)；
6. `component_uncertainty` = `sigmoid(c)·(1-sigmoid(c))` 的均值；
7. `geometry_magnitude` = 几何回归绝对值均值；
8. 六路输入进 fusion_head：

```
final_logit = max(local_top, global_logit) + 0.5·tanh(fusion_head(六路输入))
```

> 设计解读：`max(local_top, global_logit)` 是"主判定"——局部最可疑区域和全局判定取大者，保证小缺陷和大缺陷都能触发；`fusion_head` 的输出经过 `0.5·tanh` 限制在 (-0.5, 0.5)，作为软修正项。这个"max + 小幅度修正"的设计避免融合头喧宾夺主。

### 5.4 域对抗（Domain Adversarial）

`domain_head` 前面有 `GradientReversal`（梯度反转层，`grl`）：前向时原样通过，反向时梯度取反。
训练时（`adapt` 的 stage3 阶段，见后文）：真实样本 domain=0，合成异常样本 domain=1，
域分类损失让 domain_head 学会区分真假异常；梯度反转让骨干学到**"real/synthetic 域无关"的异常特征**——即只关注"异常本身"，而不依赖合成异常的人为痕迹。

### 5.5 视频时序头：TemporalLogicHead

- GRU：输入 8 维帧特征（global_score、local_score、前 3 维组件状态、前 3 维几何）→ 64 维隐状态；
- 两个输出：`state_logits`（每帧 8 状态分类，用于理解装配状态序列）+ `sequence_logit`（整个序列的异常分数）；
- 可选：`deployment/temporal_head.pth` 存在则加载，否则时序头不启用，只做逐帧检测。

---

## 6. 问题一：实时检测怎么解决

对应文件：`modules/realtime_detection.py`（类 `AOIRealtimeDetector`）。

### 6.1 单图推理 `inspect_image`：完整流程（realtime_detection.py:141-414）

**第 1 步：全图唯一一次骨干前向**
- 原图（可能 2500×2500）resize 到 384×384（ImageNet 归一化），跑一次模型，得到：
  - `supervised_heatmap`：local_logits 过 sigmoid → 24×24 热力图（可上采样回原图分辨率定位）；
  - `supervised_global`：final_logit（监督头判分）。
- **关键优化：全图只跑一次骨干**，之后所有分支复用这一次前向的特征，不重复计算。

**第 2 步：直接复用本次前向的 F16/F32 特征做正常参考评分**
- 从 `output["features"]["f16"]` 直接取 token（`f16.permute→reshape` 成 N×384），从 `f32` 取全局池化特征；
- LAB 颜色统计、几何统计直接在图（原分辨率）上算；
- `score_reused_features()` 在 GPU 上做局部 token 最近邻检索 + 马氏距离，返回 **z-score 化**的四路分数；
- 若 `enable_memory_local=false` 则走 `score_fast_features`（跳过局部检索，local 分数置 0）。

**第 3 步：ROI 精修（默认关闭）**
- `enable_roi_refinement=true` 时：按热力图 top-k 位置从**原图**裁 `roi_size×roi_size`（384）的块，再跑一次骨干，取 ROI 的 final_logit；
- 代价是额外一次前向，第一轮为保延迟直接关闭。开启时最终监督分数取 `max(全局, 最强ROI)`。

**第 4 步：五路分数加权融合**（realtime_detection.py:312-333）

```
fused_score = 1.00 × supervised_score        # 监督头
            + 0.35 × memory_local (z-score)  # 局部 token 最近邻
            + 0.35 × memory_global (z-score) # 全局马氏距离
            + 0.15 × color       (z-score)   # 颜色马氏距离
            + 0.15 × geometry    (z-score)   # 几何马氏距离
```

> 为什么要五路融合？因为缺陷形态差异极大：小划痕（局部 token 距离）、整体变色（颜色）、尺寸错位（几何）、大区域变形（全局马氏）、还有监督头见过的缺陷形态。任何一路"亮"都不应被漏掉，加权融合 + 阈值判定是"软 OR"。

**第 5 步：判定与延迟统计**
- `is_anomaly = fused_score >= threshold`（阈值来自 `normal_reference.threshold`）；
- 返回 JSON：分数、阈值、主导分支（哪一路贡献最大，用于调试）、ROI 框、**延迟拆解**（骨干前向 / 参考库 / ROI / 其他），方便精确找瓶颈。

### 6.2 视频推理 `inspect_video`（realtime_detection.py:556-656）

- 逐帧抽取（`frame_stride=5` 隔帧处理，上限 200 帧）；
- 每帧：模型前向 → 记录 global_score、local_score、组件前 3 维、几何前 3 维拼成 8 维特征；
- 若时序头存在：8 维帧特征序列 → GRU → `sequence_logit` + 每帧状态；
- 最终：`final_score = max(逐帧最大分, 时序分)`，与阈值比较判定整个视频异常。

### 6.3 批量评估 `evaluate_dataset`（realtime_detection.py:425-554）

- 输入三组路径：正常（label 0）/ seen 异常（label 1）/ unseen 异常（label 1）；
- 首张图 warm-up（不计入统计，排除 CUDA 冷启动）；
- 输出指标：**overall / seen / unseen 三个 AUROC**、准确率、精确率/召回率/F1、正常 FPR、seen/unseen 召回率、平均延迟、P95 延迟；
- 落盘：`predictions.csv`（每张图逐行）、`failed_cases.csv`（判错的）、`metrics.json`（汇总）。

> **评估口径提醒**：指标分 seen / unseen 两档看。unseen AUROC 衡量的是系统对"训练时从未见过的缺陷类型"的泛化能力——这是这个方案最看重的能力，因为产线会不断出现新缺陷。

---

## 7. 问题二：少样本/无样本启动怎么解决

对应文件：`modules/fewshot_transfer.py`（类 `FewShotTransfer`）+ `modules/normal_reference.py` + `modules/synthetic_engine.py`。

问题 2 被拆成两个阶段解决：**先在公开数据集上学"通用的异常理解"（可迁移能力），再用 100+30 张目标样本做"领域适配"（具体到这条产线）**。

### 7.1 阶段 A：公共工业预训练 `pretrain_public`

**数据**：MVTec AD + VisA + DAGM 2007 + MVTec LOCO AD 四个数据集的正常/异常图（当前环境有 mvtec_ad 和 dagm2007，缺 visa/loco 不报错，只是少数据）。

**数据增强**（`PublicIndustrialDataset`）：随机水平翻转、亮度 ±10%、对比度 ±10%；DAGM/VisA 等有像素级 mask 的异常样本加载 mask 参与分割监督（`_load_mask`）。

**类别不均衡**：`WeightedRandomSampler` 按标签权重（1/样本数）采样，保证每个 batch 正常/异常比例接近均衡。

**可训练范围**（`_set_public_trainable`）：**所有 6 个任务头 + 骨干 stage 2/3**（深层特征 + 任务头），stage 1/2 的浅层特征保留 ImageNet 预训练权值不动。学习率分层：头 2e-4，骨干 1e-5。

**多任务损失**（`_public_loss`，fewshot_transfer.py:302-399）：一张图同时被六种监督训练：

| 损失项 | 权重 | 学什么 |
|---|---|---|
| 分类（final_logit BCE） | 1.0 | 整图异常与否 |
| 全局（global_logit BCE） | 0.5 | 全局视角异常 |
| 局部（local top-1% 分数 BCE） | 0.5 | 局部最可疑区域 |
| 分割（有 mask 时 BCE + Dice） | 0.7 | 逐像素定位（有标注才生效） |
| 组件（structure 类任务的 component 均值 BCE） | 0.2 | 缺件/错位等结构异常 |
| 几何（smooth L1 回归） | 0.05 | 几何统计量回归 |
| 排序（ranking，异常分 > 正常分 + 边距） | 0.2 | 分数的相对序关系 |

**训练机制**：AMP（fp16 自动混合精度）+ 梯度裁剪(1.0) + 每 epoch 在公共验证集上算 AUROC，**保存验证 AUROC 最高的权重**（early stop 模型选择），同时写 `.history.json` 训练曲线。

> 预训练的价值：让 6 个任务头 + 深层骨干在**成百上千类工业缺陷**上学会"异常长什么样"。之后到了只有 100+30 张图的新产线，头已经有很强的异常先验，只需小幅微调，不需要从零学——这就是少样本可行的根基。

### 7.2 阶段 B：目标少样本适配 `adapt`

**输入**：100 张正常（support/normal）+ 30 张异常（support/anomaly，从 seen 缺陷类型里均衡采样）。

**门槛**：`len(normal) >= 20` 且 `len(anomaly) >= 2`（fewshot_transfer.py:841-848）。

**数据切分**：正常留 20% 做验证（阈值校准用），异常留 20% 做验证；其余训练。

**四阶段微调**（fewshot_transfer.py:866-901），每阶段只解锁一部分参数、用不同学习率，逐层深入：

| 阶段 | 解锁范围 | 学习率 | epoch | 目的 |
|---|---|---|---|---|
| 1. heads | 6 个任务头 | 1e-4 | 3 | 先把头"钉"到目标域 |
| 2. stage4 | 头 + 骨干 stage4 | 1e-5 | 3 | 最深特征适配 |
| 3. stage3+合成 | 头 + stage4 + stage3 最后 2 个 block | 3e-6 | 3 | 中层特征适配 + 合成异常对抗 |
| 4. heads 修正 | 6 个头 | 1e-4 | 2 | 最终校准 |

**合成异常**（阶段 3，概率 35%）：正常样本以 35% 概率被合成引擎污染成"伪异常"（见第 10 节），补充目标域异常多样性；此时 domain_head 开启（GRL 梯度反转 + 0.05 权重的域分类损失），防止骨干学到合成痕迹。

**目标域损失** = 分类 + 0.25×全局 + 0.25×局部 + 0.05×域对抗（fewshot_transfer.py:760-765）。

**训练机制**：每阶段都用目标验证集算 AUROC，存最优权重；AMP + 梯度裁剪。

### 7.3 适配后：建参考库 + 定阈值（adapt 的收尾，fewshot_transfer.py:903-947）

1. `reference.fit(model, train_normal)`：用训练正常图建正常参考库（token 库 + 马氏统计 + 校准 z-score）；
2. `_combined_scores(validation_normal)`：**用与最终部署完全相同的 `AOIRealtimeDetector.inspect_image` 流程**对验证正常图打分（保证校准口径与部署一致，代码注释明确强调"不能再单独实现一套"）；
3. **conformal 分位点定阈值**（fewshot_transfer.py:933-947）：

```
target_fpr = 0.05            # 允许的正常误报率
rank = ceil((n+1)·(1-0.05)) - 1
threshold = 排序后的验证正常分[rank]
```

即：阈值设在"验证正常图中第 95 分位"处，保证正常品误报率理论上 ≤ 5%（有限样本下的 conformal 修正）。100 张里留的 20 张验证正常图 ≈ 17 张时，rank≈16，取第 17 大的分数当阈值。

4. 保存：`target_model.pth`（模型权重）+ `normal_reference.pth`（参考库 + 阈值 + 校准）。

### 7.4 无样本（zero-shot）启动的现状

- 代码里已有 `build_zero_shot_reference`（fewshot_transfer.py:237）——**不训练、只用正常图建参考库**；
- `NormalReference.fit` 最低要求 5 张正常图（normal_reference.py:115）；
- **但 main.py 没有暴露 zero-shot 命令**，且 `adapt` 有 20 正常 + 2 异常 的硬门槛。所以当前"无样本"路径是：跳过适配，直接加载 `pretrain-public` 的模型 + 用公共数据或少量目标正常图建参考库 + 公共校准阈值——这是可行的（代码支持），只是还没有命令行封装。**这正是前面讨论"少样本/无样本启动"时给出的第一个落地建议**。

### 7.5 为什么这套能解决少样本

三层递进：
1. **特征不动**：浅层 stage1/2 全程冻结，保留 ImageNet 通用视觉能力；
2. **头先学**：预训练已给头灌入大量异常先验，目标域只需 3+2 epoch 微调；
3. **正常性建模兜底**：即使监督头对未见缺陷"没反应"，正常参考库的最近邻/马氏距离会把偏离正常分布的像素揪出来——unseen 缺陷的检出主要靠这一路（当前实验 unseen AUROC 0.9167 就是这么来的）。

---

## 8. 问题三：用户反馈优化怎么解决

对应文件：`modules/feedback_optimization.py`（类 `FeedbackOptimizer`）。

赛题要求"反馈只用在独立更新阶段，更新必须经过验证和回滚检查"。代码实现分两层：

### 8.1 即时更新（不改模型）

- `add_feedback`：把每条反馈（图片路径、模型预测标签、人工纠正标签、预测分数、缺陷类型、备注、时间戳）追加写进 `feedback/feedback.jsonl`；
- `threshold_suggestion`：统计历史误报（pred=1, correct=0）分数中位数 +0.05、漏报（pred=0, correct=1）分数中位数 -0.05、当前阈值，三者取中位数作为建议阈值；
- `apply_immediate_update`：**单次阈值调整幅度限制在 ±0.1**（防止少量反馈让阈值剧烈跳动），只动阈值、不碰模型参数——符合"反馈不直接改线上模型"。

### 8.2 周期更新（重训 + 回滚）

`periodic_retrain`（feedback_optimization.py:160-250）：
1. **门槛**：反馈累计 ≥ 20 条（`feedback_retrain_min_samples`）才触发；
2. **备份**：先复制当前 `target_model.pth` 和 `normal_reference.pth` 到 `feedback/rollback/model_时间戳.pth`；
3. **重训**：用"原始 100+30 + 纠正后的反馈样本（去重）"重新跑 `transfer.adapt`（禁用合成引擎，只微调头/Stage4）——这就是**历史回放**；
4. **安全检查**：新阈值与旧阈值偏差 > 0.5 视为异常，触发回滚；
5. **回滚**：任何异常（训练失败、阈值漂移）→ 从备份恢复模型与参考库。

---

## 9. 数据划分：seen / unseen 实验设计

对应文件：`utils/paths.py` 的 `build_public_and_target_split`（main.py 的 `make-split` 命令）。

### 9.1 公共池 vs 目标类

- 扫描四数据集 → 全部记录（path/label/mask/类别/任务类型）；
- **目标类** = 指定的 (dataset, category)（当前：mvtec_ad 的 grid）；
- **公共池** = 其余所有类别（**严格排除目标类**，防泄漏）；
- 公共池按"类别"为最小单位切分：每个数据集的类别随机抽 ~20%（至少 1 个）做公共验证，其余做公共训练（`public_train.jsonl` / `public_val.jsonl`）——保证验证集类别训练时完全没见过。

### 9.2 目标类：support / query 三路

以当前实验（grid 目标，bent 为 unseen）为例：

| 集合 | 来源 | 数量 | 用途 |
|---|---|---|---|
| support/normal | grid 的 train/good 前 100 张 | 100 | 少样本适配的正常侧 |
| support/anomaly | broken/glue/metal/thread 四类缺陷**按缺陷类型均衡**取 30 张 | 30 | 少样本适配的异常侧（"已见"缺陷） |
| query/normal | grid 的 test/good | 21 | 测试正常 |
| query/seen | 已见缺陷剩余全部 | 15 | 测试已见异常 |
| query/unseen | **bent（完全没进 support）** | 12 | 测试未见异常 |

**关键设计**：把 bent 类型从 support 里彻底剔除（`query_unseen` 的筛选是 `defect_type == unseen_type`，而 support 从 `seen_pool` 里取），所以 bent 对适配流程完全未知——用它测"未见缺陷泛化"。

### 9.3 落盘形式

- 7 个 JSONL 清单 + `summary.json` 统计；
- `support/`、`query/` 目录下用**符号链接**（失败则复制）把图片按新命名（`序号_数据集_类别_缺陷_原文件名`）组织好，方便直接喂给训练/推理命令。

---

## 10. 合成异常引擎

对应文件：`modules/synthetic_engine.py`（类 `SyntheticEngine`）。用途：少样本适配阶段**给正常样本"造"异常**，弥补目标域真实异常只有 30 张、形态不足的问题。

| 方法 | 合成什么 | 做法 |
|---|---|---|
| `appearance` | 局部裂纹/污渍 | 随机线段 + 暗色（RGB<90），同时产出 mask |
| `color` | 整体颜色异常 | 色饱和度 0.55~1.55 倍 + 亮度 0.75~1.25 倍 |
| `geometry` | 尺寸/错位 | 随机 x/y 方向缩放 0.85~1.15 后居中贴回画布，产生"变形" |
| `component_missing` | 缺件 | 对指定区域做 cv2.inpaint 修复（模拟部件消失），预留接口 |
| `appearance_generator` | 外部 GAN 接入点 | 预留 DFMGAN/StyleGAN 接口，未接入 |

在 `TargetDataset.__getitem__` 里，正常样本以 `synthetic_probability`（默认 35%）概率随机挑一种合成方式，**标签翻转为 1、domain 标记为 1**，与真实异常（domain 0）一起训练，配合域对抗头使用。

---

## 11. 阈值是怎么定的

阈值的完整生命周期（三处协同）：

1. **适配时（conformal）**：验证正常图按部署打分流程排序，取 `ceil((n+1)(1-fpr))-1` 分位 → `normal_reference.threshold`（第 7.3 节）；
2. **部署时**：`fused_score >= threshold → anomaly`，阈值冻结；
3. **反馈时**：`apply_immediate_update` 用误报/漏报分数中位数做建议，每次最多 ±0.1；`periodic_retrain` 重训后若新阈值偏离旧阈值 > 0.5 则回滚。

当前实验（见第 16 节）在 5% 目标 FPR 下实测正常 FPR = 0.0%（21 张正常全对），seen 召回 60%，unseen 召回 16.7%——阈值偏保守，宁可漏报少误报。

---

## 12. 延迟与精度：2500×2500 高分辨率输入怎么办

### 12.1 当前实测

- **384×384 输入（`evaluation/metrics.json`）**：平均 149.1 ms、P95 177.9 ms（RTX 3060 时代测的）；
- **2500×2500 输入（RTX 5060，2026-08-13 复测）**：修复前完整管线 ~430 ms（其中 `lab_statistics` 在全分辨率上花 271 ms）；修复后 ~191 ms（median）/ 200 ms（P95）。

满足赛题 GPU 单图 <200 ms 的目标。**注意：2500×2500 是"输入图片"尺寸，骨干实际在 384×384 上跑**；延迟考核是端到端的（读图 → 特征 → 骨干 → 参考库 → 判定）。

### 12.2 延迟是怎么压下来的（代码里的具体手段）

0. **CPU 特征提取先降采样（`utils/image.py`，已实现）**：`lab_statistics` 把最长边超过 512 的图等比降采样后再算 LAB 直方图（2500×2500 时 271 ms → 33 ms，特征余弦相似度 0.99999，几乎无损）；`geometry_statistics` 最长边超过 1024 时降采样（Canny/轮廓 32 ms → 28 ms，与全分辨率差异 <0.1%）。**这是 2500×2500 输入下延迟的大头，也是端到端从 ~430 ms 降到 ~191 ms 的关键修复**。改特征后正式评测前建议重新跑 `adapt`（参考库的校准统计是按旧特征拟合的）。
1. **单次前向**：全图 384×384 只跑一次骨干，参考库评分复用同一前向的 F16/F32 特征（`score_reused_features`），不二次提取；
2. **GPU token 检索**：局部最近邻用矩阵乘 + 平方展开，分块 4096，避免 CPU 暴力检索（`score_local_torch`）；
3. **AMP**：`torch.autocast(fp16)` + `GradScaler`，训练推理都开；
4. **channels_last**：C²-FFN 和骨干前向显式 `contiguous(memory_format=channels_last)`，卷积在 GPU 上更快；
5. **ROI 精修默认关闭**（多一次前向太贵），热力图本身就能定位；
6. **C²-FFN 实验模式**（`backbone_mode="c2_hard_b"`）：Stage 1–3 只在 128/32/8 个质心上跑通道 FFN 再散射回去，Stage 4 保持 dense。README 记录 P95 193.01→122.67 ms（-36.4%）、AUROC 0.6896→0.6178（-7.18 个百分点）。
   **2026-08-13 本地复现（结论更差）**：修复了 C² 在 fp16 下梯度 NaN 导致完全不学习的问题（`FewShotTransfer._autocast` 对 C² 强制 fp32），跑完整预训练→适配→评估后，公共预训练 val_auc 仅 0.58（dense 0.96）、目标域评估 AUROC **0.446**（dense 0.965，tp=3/27 几乎全漏）。质心硬路由表达能力不足 + argmax 路由不可微，C² 在现有训练配方下学不动。
   **结论：C² 不可用，精度方法一律用 dense**；端到端延迟两者几乎相同（骨干仅占管线 12ms），C² 的骨干加速在完整管线上没有意义。

---

## 13. 可视化工具 vis_infer.py

加载 `deployment/` 的模型 + 参考库，对每张图输出一张三栏对比图：

```
┌──────────────┬──────────────┬──────────────┐
│  原图         │ 异常热力图     │ F16 特征激活  │
│              │ (JET 色带叠加) │ (INVERNO 色带)│
├──────────────┴──────────────┴──────────────┤
│ 信息面板：文件名/状态/分数/阈值/主导分支/延迟   │
│ 分支分数柱状图：Supervised/LocalMem/GlobalMem/│
│              Color/Geometry（正负分色）      │
└──────────────────────────────────────────────┘
```

用法（README 30.8 节）：`--image` 单张/多张，或 `--normal-dir/--seen-dir/--unseen-dir` 按类别批量（`--max-per-type` 限制数量），输出到 `--save-dir`。

---

## 14. 命令行速查：8 个命令分别对应什么

| 命令 | 阶段 | 解决什么 | 关键参数 |
|---|---|---|---|
| `make-split` | 一 | 数据划分（公共池/目标 support/query seen/unseen） | `--target-dataset mvtec_ad --target-category grid --unseen-type bent` |
| `pretrain-public` | 一 | 四数据集公共预训练 → `industrial_pretrained.pth` | `--split-dir`（必填） |
| `adapt` | 二 | 100+30 少样本适配 → `target_model.pth` + `normal_reference.pth` | `--normal-dir --anomaly-dir`（必填），`--disable-synthetic` 可选 |
| `evaluate` | 三 | 冻结模型批量评估 → `evaluation/` 三件套 | `--normal-dir --seen-dir --unseen-dir` |
| `infer-image` | 三 | 单张图片推理（JSON 结果含延迟拆解） | `--image` |
| `infer-video` | 三 | 视频逐帧 + 时序检测 | `--video --frame-stride 5` |
| `feedback` | 四 | 记录反馈 + 即时阈值调整 | `--image --predicted-label --corrected-label --score` |
| `feedback-retrain` | 四 | 反馈满 20 条后周期重训 + 回滚 | `--normal-dir --anomaly-dir --validation-normal-dir` |

所有命令都可用 `--config` 指定配置（默认 `config.json`）。

---

## 15. 配置文件全字段说明

`config.json` ↔ `config.py` 的 `AOIConfig` 数据类（字段即 dataclass 属性）：

**路径**：`root` / `student_checkpoint`（学生权重）/ `teacher_checkpoint`（教师权重，预留）/ `workspace` / `industrial_checkpoint` / 四个数据集根路径。相对路径以 JSON 文件所在目录为基准解析。

**运行**：`device`（默认 cuda）/ `amp`（混合精度开关）。

**分辨率**：`train_size`、`global_size`、`roi_size` 都是 384；`topk_rois=2`（ROI 数）；`local_top_ratio=0.01`（热力图 top-1% 均值）。

**骨干**：`backbone_mode="dense"`（可选 `c2_hard_b`）。

**参考库**：`max_local_tokens=12000`、`local_tokens_per_image=128`、`enable_memory_local=true`、`local_bank_chunk_size=4096`、`enable_roi_refinement=false`。

**公共训练**：`public_batch_size=8`、`public_num_workers=2`、`public_epochs=5`、`public_steps_per_epoch=300`、`public_lr_head=2e-4`、`public_lr_backbone=1e-5`、`public_weight_decay=1e-4`，以及 7 个损失权重（global 0.5 / local 0.5 / segmentation 0.7 / component 0.2 / geometry 0.05 / rank 0.2）。

**目标适配**：`batch_size=8`、`head_epochs=3`、`stage4_epochs=3`、`stage3_epochs=3`、`real_correction_epochs=2`、分层学习率 `lr_head=1e-4` / `lr_stage4=1e-5` / `lr_stage3=3e-6`。

**分数融合**：`supervised_weight=1.0`、`memory_local_weight=0.35`、`memory_global_weight=0.35`、`color_weight=0.15`、`geometry_weight=0.15`。

**反馈**：`feedback_retrain_min_samples=20`、`feedback_max_memory_add=64`（预留）、`rollback_tolerance=0.01`（预留）。

**阈值**：`target_normal_fpr=0.05`、`threshold_quantile=0.95`（参考项）。

---

## 16. 当前实验结果

### 16.2 跨类别验证：transistor（unseen = bent_lead）

2026-08-13 用同一套配置（dense + 纯监督）在多个类别上重跑完整流程，验证方案可迁移性：

| 指标 | grid | leather | capsule | transistor | dagm Class3 |
|---|---:|---:|---:|---:|---:|
| Overall AUROC | 0.9929 | **1.0** | 0.9527 | 0.7233 | **0.9999** |
| Seen AUROC | 1.0 | 1.0 | 0.9674 | 0.8317 | 0.9999 |
| Unseen AUROC | 0.9841 | 1.0 | 0.9168 | 0.6150 | 无 unseen |
| 固定阈值检出 | 24/27 | **62/62** | 66/79（83.5%） | 3/20 | **120/120** |
| 正常 FPR | 0.0% | 3.1% | 8.7% | 0.0% | 5.7% |
| unseen 类型 | bent（弯折） | fold（褶皱） | crack（裂纹） | bent_lead（引脚弯折） | —（单缺陷类型） |

DAGM Class3 说明：缺陷类型只有一种（`defect`），用无 unseen 模式跑
（`--unseen-type` 省略）；目标类别从预训练数据池中完整剔除（对比 grid 划分
`public_train` 17572 → Class3 划分 16909，正好少 Class3 全部记录），无泄漏。
DAGM 图像为 512×512、缺陷为细纹理刮痕/污渍，120/120 全检出，AUROC 0.9999。

结论：
- **纹理/大面积缺陷类别（leather、grid）非常强**，几乎满分；**细线缺陷（capsule crack）中等**（unseen AUROC 0.92、检出 17/23）；**小目标缺陷（transistor 引脚）弱**（unseen 0.62、检出 0/10）——难度梯度与缺陷尺度高度相关，384×384 输入下小目标细节损失是主要瓶颈；
- 正常 FPR 随类别波动（capsule 8.7%）：conformal 阈值只用 20 张验证正常图校准，类别方差大时校准偏粗；
- 管线跨类别零改动直接跑通（划分→预训练→适配→评估→可视化）；
- `make-split` 的 `--unseen-type` 已支持省略（单缺陷类型类别如 toothbrush 时全部异常进 support）。

### 16.4 结构缺陷诊断实验（transistor，2026-08-13）

背景：transistor（引脚弯折/切断/错位）是**结构/装配类缺陷**，与其余类别的表面外观缺陷不同。
诊断实验（隔离"缺陷尺寸"与"缺陷类型"两个因素）：

| 实验 | 因素 | Overall AUROC | unseen AUROC | 结论 |
|---|---:|---:|---:|---|
| 干净基线 | — | 0.7442 | 0.6167 | 参照（预训练池排除目标类别，防泄漏） |
| E0 ROI 精修 | 尺寸 | 0.6825 | 0.7217 | ❌ 有害（seen 0.87→0.64，延迟×2） |
| E1 分辨率 512 | 尺寸 | 0.7400 | 0.7083 | ❌ 无效（capsule 对照组 0.953→0.930） |
| E2 适配结构监督 | 类型·监督 | 0.7533 | 0.6700 | ✅ 小幅正面 |
| E3 LOCO+VisA 预训练 | 类型·数据 | 0.8617 | 0.8333 | ✅✅ 大幅正面 |
| **最终组合（E3+E2）** | 两者 | **0.8692** | **0.8683** | ✅ 最优（unseen 召回 0.2→0.7） |

结论：
- **根因是"类型泛化缺数据"而非"缺陷尺寸小"**：预训练池原来 100% 是 appearance 任务（LOCO 缺失），
  component_head 从未被监督；LOCO（缺件/错位/组合错误）与 VisA 加入后 transistor unseen AUROC
  0.62→0.87，unseen 召回 0.2→0.7；
- E2 的改动已保留在代码里（`_train_phase` 增加 component 0.2 + geometry 0.05 损失，目标域异常视为组件级异常）；
- 泄漏警示：共享 `industrial_pretrained.pth` 若直接用上一类别实验的预训练权重，会包含目标类别数据，
  AUROC 虚高（实测 0.744→0.991 假象）；每个目标类别的实验必须用该类别专属 manifest 重跑预训练；
- 实验配置与产物：`config_trans_512.json`/`config_capsule_512.json`/`config_trans_sup.json`/`config_roi.json`，
  部署备份 `deployment_backup_trans_clean/`（基线）、`deployment_backup_e3/`（E3）、
  `deployment_backup_dagm/` 等；最终组合模型为当前默认部署。

### 16.3 可视化产出

| 类别 | 目录 | 张数 |
|---|---|---:|
| grid | `vis_results/` | 48 |
| transistor | `vis_results_transistor/` | 50 |
| leather | `vis_results_leather/` | 77 |
| capsule | `vis_results_capsule/` | 76 |

### 16.1 最近一次评估（`aoi_full_workspace/evaluation/metrics.json`）

评估集 48 张（21 正常 + 15 seen + 12 unseen，mvtec_ad grid / bent unseen）。
2026-08-13 三次演进对比（部署版本：原始 → 特征降采样+重校准 → **完全关闭参考库**）：

| 指标 | 原始 | 降采样+重校准 | **纯监督（现状）** |
|---|---:|---:|---:|
| Overall AUROC | 0.9453 | 0.9647 | **0.9929** |
| Seen AUROC | 0.9683 | 0.9873 | **1.0000** |
| Unseen AUROC | 0.9167 | 0.9365 | **0.9841** |
| 准确率 | 0.667 | 0.750 | **0.938** |
| F1 | 0.579 | 0.714 | **0.941** |
| 正常 FPR | 0.0% | 0.0% | **0.0%** |
| Seen 召回 | 60.0% | 66.7% | **93.3%** |
| Unseen 召回 | 16.7% | 41.7% | **83.3%**（10/12） |
| 平均延迟 | 149.1 ms | 48.1 ms | **45.3 ms** |
| P95 延迟 | 177.9 ms | 52.2 ms | **48.8 ms** |
| 阈值 | — | 6.2M（融合分） | **0.3301（监督分）** |

解读：
- **第三版（纯监督）全面最优**：消融实验发现正常参考库的 `memory_local` 分支原始距离是百万级数值，在融合中淹没监督分（±几十），反而拖累精度（unseen 召回 0.50→0.83 的提升来自关掉它）；`memory_global` 单独加还会把正常 FPR 推到 28.6%。在 grid 单类别 48 张评估上，参考库净收益为负；
- 实现方式：`config.json` 的 `enable_reference=false` —— 适配时跳过 `reference.fit`、阈值按纯监督分 conformal 校准（0.3301），推理时跳过参考库评分；
- **注意口径**：该结论基于 n=48、单类别（grid）。其他类别或监督头覆盖不到的新形态缺陷场景下，参考库仍可能有用——`enable_reference=true` 可随时切回；
- 历史存档：`evaluation/metrics_pre_downscale.json`、`deployment_backup_pre_downscale/`（原始版）、`deployment_backup_ref_based/`（参考库版）。

### 16.2 公共预训练历史（`industrial_pretrained.history.json`）

5 epoch 训练曲线：train_loss 从 1.62 降到 0.31，val AUROC 稳定在 0.963~0.966（注意 val_loss 后期上升是过拟合信号，但 AUROC 未见下降，模型选择了最佳 AUROC 权重）。

### 16.3 已知问题 / 限制

- `visa/`、`mvtec_loco_ad/` 数据集缺失 → 公共预训练实际只用 mvtec_ad + dagm2007（扫描容错，不报错）；
- `summary.json` 里 split 路径是 Linux 风格（`/mnt/wang/AOI/...`，跨机器产生），Windows 下重跑 `make-split` 会按当前机器重新生成；
- `vis_infer.py` 构建模型时没传 `backbone_mode`（默认 dense）——如果部署模型是 C² 训练的，可视化会加载不上，需要同步修改；
- `evaluate` 的阈值是部署时的固定阈值，指标反映的是"部署点"而非"最大 AUROC 点"。

---

## 17. 关键实现细节速查表

| 文件 | 类/函数 | 一句话职责 | 关键行 |
|---|---|---|---|
| main.py | `build_model` | 建模型，可加载完整权重 | :23 |
| main.py | `load_deployed` | 加载 deployment/ 的模型 + 参考库 | :71 |
| main.py | 8 个 `command_*` | 各命令的入口 | :117 起 |
| aoi_model.py | `AOIMultiBranchModel` | 1 骨干 + 6 头，final_logit 融合 | :27 |
| aoi_model.py | `grl` | 梯度反转（域对抗） | :23 |
| aoi_model.py | `TemporalLogicHead` | 视频 GRU 时序头 | :172 |
| convnextv2.py | `ConvNeXtV2Tiny` | 四尺度骨干 f4/f8/f16/f32 | :83 |
| convnextv2.py | `load_convnextv2_tiny` | 权重加载 + 前缀清理 + 严格检查 | :185 |
| config.py | `AOIConfig` | 配置数据类 + 路径解析 + 目录创建 | :10 |
| utils/image.py | `normalize_image` | resize + ImageNet 归一化 | :25 |
| utils/image.py | `lab_statistics` | 54 维 LAB 颜色特征 | :35 |
| utils/image.py | `geometry_statistics` | 6 维几何特征 | :59 |
| utils/image.py | `nms_boxes` | ROI 框 NMS | :90 |
| utils/paths.py | `scan_all_public_datasets` | 四数据集扫描合并 | :310 |
| utils/paths.py | `build_public_and_target_split` | seen/unseen 划分全套 | :445 |
| modules/realtime_detection.py | `AOIRealtimeDetector.inspect_image` | 单图完整检测流水线 | :141 |
| modules/realtime_detection.py | `evaluate_dataset` | 三组 AUROC + CSV 落盘 | :425 |
| modules/realtime_detection.py | `inspect_video` | 视频逐帧 + 时序 | :556 |
| modules/normal_reference.py | `fit` | 建四路正常参考 + 校准 | :101 |
| modules/normal_reference.py | `score_local_torch` | GPU 分块最近邻检索 | :323 |
| modules/normal_reference.py | `score_reused_features` | 复用前向特征评分（z-score） | :588 |
| modules/normal_reference.py | `zscore` | 分支分数标准化 | :253 |
| modules/fewshot_transfer.py | `pretrain_public` | 公共多任务预训练 + 最佳模型选择 | :439 |
| modules/fewshot_transfer.py | `_public_loss` | 7 项多任务损失 | :302 |
| modules/fewshot_transfer.py | `adapt` | 四阶段适配 + 建库 + conformal 阈值 | :833 |
| modules/fewshot_transfer.py | `_train_phase` | 单阶段微调（分层解锁 + 分层 LR） | :667 |
| modules/fewshot_transfer.py | `_combined_scores` | 用部署同款流程打分校准 | :799 |
| modules/synthetic_engine.py | `SyntheticEngine` | 四类合成异常 | :16 |
| modules/feedback_optimization.py | `apply_immediate_update` | 阈值 ±0.1 即时调整 | :139 |
| modules/feedback_optimization.py | `periodic_retrain` | 重训 + 备份 + 回滚 | :160 |
| modules/c2_ffn_convnext_v1.py | `C2FFNConvNeXtV1` | 质心 FFN 加速骨干（实验） | :162 |

---

## 附：运行环境与常见事项

- 运行环境：conda `New_env`（torch 2.7.0+cu128，CUDA 可用），base 环境无 torch；
- 完整运行顺序：`make-split → pretrain-public → adapt → evaluate → infer-image/infer-video → feedback/feedback-retrain`（命令细节见 README 第 29 节和本文件第 14 节）；
- `aoi_full_workspace/` 全部是运行产物，可删除重建（`make-split` 会重建目录）；
- 缺失的数据集目录只是少数据，不会报错；
- C²-FFN 与 Dense 权重不通用，切换 `backbone_mode` 需重训。
